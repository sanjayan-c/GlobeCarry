<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/bluegray_100_87" />
            <corners android:radius="20dp" />
            <stroke android:color="@color/bluegray_100_87" android:width="2dp" />
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/my_grey" /> <!-- Change to the color you want when pressed -->
            <corners android:radius="20dp" />
            <stroke android:color="@color/my_grey" android:width="2dp" /> <!-- Change to the color you want when pressed -->
        </shape>
    </item>
</selector>
