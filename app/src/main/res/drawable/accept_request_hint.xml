<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/green" />
            <corners android:radius="20dp" />
            <stroke android:color="#2D5004" android:width="2dp" />
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#7DC180" /> <!-- Change to the color you want when pressed -->
            <corners android:radius="20dp" />
            <stroke android:color="#90AC70" android:width="2dp" /> <!-- Change to the color you want when pressed -->
        </shape>
    </item>
</selector>
