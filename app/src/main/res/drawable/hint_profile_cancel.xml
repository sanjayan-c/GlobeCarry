<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/red_A200_aa" />
            <corners android:radius="20dp" />
            <stroke android:color="@color/red_A200_aa" android:width="2dp" />
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/red_A200_aaa" /> <!-- Change to the color you want when pressed -->
            <corners android:radius="20dp" />
            <stroke android:color="@color/red_A200_aaa" android:width="2dp" /> <!-- Change to the color you want when pressed -->
        </shape>
    </item>
</selector>
